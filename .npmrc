registry=http://127.0.0.1:4873
link-workspace-packages = true
# 关闭workspace-lockfile的锁，让每一个package拥有自己lockfile
# 如果共享lock包的话，保证版本的唯一，但是因为pnpm 到后期会分析依赖，就是有多少可以refuse，速度变慢
# 不共享lock包， 就是速度快，但是每一个都有自己的lock包

shared-workspace-lockfile = false